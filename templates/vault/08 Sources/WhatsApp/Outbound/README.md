# WhatsApp Outbound

Outbound sending is disabled unless you explicitly run the sender with `--yes`.

Draft:

```bash
selfprint send-whatsapp --vault . --to "Name" --message "Text"
```

Send:

```bash
selfprint send-whatsapp --vault . --to "Name" --message "Text" --yes
```

