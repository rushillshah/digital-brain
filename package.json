{
  "name": "digital-brain",
  "version": "1.5.0",
  "description": "Your private digital imprint for AI assistants.",
  "type": "module",
  "bin": {
    "digital-brain": "bin/digital-brain.js"
  },
  "files": [
    "bin",
    "lib",
    "scripts/*.py",
    "templates",
    "whatsapp-web",
    "slack",
    "imessage",
    "examples",
    "docs",
    "README.md",
    "SECURITY.md",
    "PRIVACY.md",
    "ROADMAP.md",
    "CONTRIBUTING.md",
    "LICENSE"
  ],
  "scripts": {
    "test:sample": "node ./bin/digital-brain.js extract --vault ./examples/sample-vault --days 365 --min-messages 1 && node ./bin/digital-brain.js interpret --vault ./examples/sample-vault --days 365",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check bin/digital-brain.js && node --check lib/*.js && node --check whatsapp-web/*.mjs && node --check slack/*.mjs && node --check imessage/*.mjs && node --check desktop/main.mjs && node --check desktop/preload.cjs && node --check desktop/renderer/*.js && python3 -m py_compile scripts/*.py",
    "start": "node ./bin/digital-brain.js",
    "desktop": "electron desktop/main.mjs"
  },
  "keywords": [
    "ai",
    "memory",
    "personal-ai",
    "obsidian",
    "whatsapp",
    "imessage",
    "local-first",
    "context"
  ],
  "author": "Rushill Shah",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.34.7"
  },
  "devDependencies": {
    "electron": "^42.4.0"
  }
}
